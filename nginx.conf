server {
  listen 80;
  server_name _;

  # Redirect /graph-js → /graph-js/
  location = /graph-js {
    return 301 /graph-js/;
  }

  # Serve the Vue app under /graph-js/
  location /graph-js/ {
    alias /usr/share/nginx/html/;
    try_files $uri $uri/ /index.html;
  }
}